import { useFrame, useThree } from "@react-three/fiber";
import type { ReactNode } from "react";

function SelectedPosition(): ReactNode {
  const { camera } = useThree()
  useFrame((state, delta) => {

  })
  return ""
}

export default SelectedPosition